#description of input fields
# type: title, file, string, float, int, switch (NO (default),YES), combo (for selection list)
# descr: description of the parameter lable
# tooltip: detailed description shown when mouse cursor remains over label
# default: optional default value for parameter
# min: optional minimum value
# max: optional maximum value
# column: column of parameter in gui (0-2),default = 0, title and file are always streched over all columns 
# prefix: prefix for parameter in pipe
space:
  - Name:
      descr:   "name" 
      type:    "string" 
  - Distance:                  # -d   0.0  [cm]   length of the free flight path along the x-axis 
      type:    "float" 
      descr:   "distance [cm]"
      tooltip: "length of the free flight path (along the x-axis)"
      default: 0.0
      min:     0.0
      column:  0                     
      prefix:  "-d"     
  - MyScat:                  # -M   0.0 [1/cm]  macroscopic scattering coefficient 
      type:    "float" 
      descr:   "total scat-\ntering [1/cm]"
      tooltip: "macroscopic total scattering coefficient"
      default: 0.0
      min:     0.0
      column:  1                     
      prefix:  "-M"     
  - MyAbs:                  # -m   0.0 [1/cm]  macroscopic absorption coefficient 
      type:    "float" 
      descr:   "absorption\n[1/cm]"
      tooltip: "macroscopic absorption cross-section for 1.798 Ang" 
      default: 0.0
      min:     0.0
      column:  2                     
      prefix:  "-m"     

